# Artisyn (Monorepo)

Artisyn is a Artisan listing platform with:
- **Web App (React)** in `client/`
- **Backend API (Node/Express + MongoDB)** in `server/`

This repo is structured as a **monorepo** so both frontend and backend live in one place.

---

## Folder Structure

